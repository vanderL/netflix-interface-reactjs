import React from 'react'
import Tmdb from './Tmdb'

export default () => {

  useEffect(() =>{
    const loadAll = async () => {
      //Pegando a lista!
    }

    loadAll()
  }, [])

export default () => {
  return (
    <div>
      Olá Mundo!
    </div>
  )
}